# Release QA Report Template

Use this template to record a release candidate QA run. Store completed reports alongside release notes.

## Release Metadata
- Date: 2026-02-07
- Calliope version/build: Not run (dist/Calliope.app missing)
- macOS version: macOS 26.2 (Build 25C56)
- Environment (Physical/VM): Not run (requires local macOS test machines)
- Hardware (Apple Silicon/Intel + model): Apple Silicon (Apple M4 Pro, 14-core, 48 GB)
- Microphone device: Not run (requires physical hardware)
- Reviewer: Ralph (automated agent)

## Build & Run
- `swift build` succeeds: Not run (packaged app verification requires local macOS test machines)
- `swift run` launches and defaults to Session screen: Not run (packaged app verification requires local macOS test machines)
- First launch renders without missing assets: Not run (packaged app verification requires local macOS test machines)

## Permissions Flow
- Microphone prompt appears on first Start: Not run (requires local macOS test machines)
- Denied mic shows Settings recovery actions: Not run (requires local macOS test machines)
- Granted mic enables session + input level: Not run (requires local macOS test machines)
- Speech recognition status shows in Settings and gating works: Not run (requires local macOS test machines)

## Session Lifecycle
- Start updates status to Recording: Not run (requires local macOS test machines)
- Stop shows post-session recap: Not run (requires local macOS test machines)
- Optional title prompt appears and saves: Not run (requires local macOS test machines)
- Active coaching profile label updates during session: Not run (requires local macOS test machines)
- Minimize or background the app during recording; session continues and timers stay accurate: Not run (requires local macOS test machines)
- Close the window mid-session, reopen Calliope, and confirm the active session UI restores with correct elapsed time: Not run (requires local macOS test machines)

## Live Feedback
- Pace, crutch words, pauses update while speaking: Not run (requires local macOS test machines)
- Input level is responsive: Not run (requires local macOS test machines)
- Latency/risky audio warnings appear when triggered: Not run (requires local macOS test machines)
- Feedback panel remains minimal and calm: Not run (requires local macOS test machines)

## Recordings & Playback
- Recording appears after Stop: Not run (requires local macOS test machines)
- Detail sheet shows stats: Not run (requires local macOS test machines)
- Playback sounds like only the local speaker: Not run (requires local macOS test machines)
- Reveal/Open Folder works: Not run (requires local macOS test machines)

## Diagnostics Export
- Export generates JSON under diagnostics folder: Not run (requires local macOS test machines)
- JSON contains no audio or transcript data: Not run (requires local macOS test machines)

## Packaging & Artifacts
- `./scripts/build-app.sh` produces `dist/Calliope.app`: Pass (2026-02-07; app bundle created in `dist/Calliope.app`)
- `./scripts/package-release.sh` produces `dist/Calliope.app` + versioned zip: Pass (2026-02-07; zip created at `dist/Calliope-1.0.0.zip`; unsigned)
- Signing identity applied when configured: Not run (requires configured signing identity)

## Packaged App Verification (Per macOS Version)
Fill one line per macOS version tested. Validate launch, permissions, recordings storage, and session flows.

| macOS Version | Environment | Machine | Launches | Permissions | Session Flow | Recordings Storage | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| macOS 13 (Ventura) | Not run (automation host lacks macOS 13) | Not run (automation host lacks macOS 13) | Not run | Not run | Not run | Not run | Blocked 2026-02-07: packaged app verification requires access to a macOS 13 test machine or VM. |
| macOS 14 (Sonoma) | Not run (automation host lacks macOS 14) | Not run (automation host lacks macOS 14) | Not run | Not run | Not run | Not run | Blocked 2026-02-07: packaged app verification requires access to a macOS 14 test machine or VM. |
| macOS 15 (Sequoia) | Not run | Not run | Not run | Not run | Not run | Not run | Blocked: requires packaged app verification on macOS 15 |

## Notarization & Signing
- Notarization submission command recorded (if applicable): Not run (requires signing credentials)
- Staple step completed (if applicable): Not run (requires notarization)

## Performance Validation
- Completed `PERFORMANCE_CHECKLIST.md` and attached results: Not run (requires local macOS test machines)

## Privacy Confirmation
- No audio or transcripts leave the device: Not run (requires local macOS test machines)
- Only the local microphone input is captured: Not run (requires local macOS test machines)

## PRD Acceptance Checklist
Confirm the core PRD requirements and success metrics are met during this QA run.

### Core Requirements
- Live session shows elapsed time, speaking time, pace (WPM), crutch word count, pause indicator, and input level while recording: Not run (requires local macOS test machines)
- Closed captions are visible by default and the CC toggle works: Not run (requires local macOS test machines)
- Crutch counts in live feedback match the final session summary: Not run (requires local macOS test machines)
- Coaching profile selection is available per session (including per-app profiles) and affects targets: Not run (requires local macOS test machines)
- Session recordings save locally with date/time-based default names and optional titles: Not run (requires local macOS test machines)
- Recordings list supports search and sorting (Date, Duration, Speaking %): Not run (requires local macOS test machines)
- Recording details include speaking time, speaking turns, pace, crutch counts, and pauses: Not run (requires local macOS test machines)

### Success Metrics
- Start a session within 2 seconds of app launch: Not run (requires local macOS test machines)
- Live feedback is understandable at a glance without training or docs: Not run (requires local macOS test machines)
- The UI feels calm, focused, and native during a live call: Not run (requires local macOS test machines)

## User-Facing Release Notes
### What Changed
- Pending QA run on supported macOS versions.

### Known Issues
- Packaged app verification not yet performed on macOS 13/14/15.

### Support / Contact
- Pending QA run completion.
